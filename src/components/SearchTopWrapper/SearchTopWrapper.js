import React from 'react';

class SearchTopWrapper extends React.Component {
    render(){
        return (
            <div>
                {this.props.children}
            </div>

        )
    }

};

export default SearchTopWrapper;